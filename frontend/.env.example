# Copy to .env for the frontend (all optional).
# Where the backend lives when NOT using the dev proxy. Leave empty to use the
# Vite proxy (/api -> http://localhost:8000).
VITE_API_BASE=

# Used only by vite.config.js dev proxy target.
VITE_API_TARGET=http://localhost:8000
