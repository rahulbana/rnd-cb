# Backend origin. Leave empty in dev (Vite proxies /api to the backend).
# In production set this to your backend URL, e.g. https://api.example.com
VITE_API_BASE=

# Default number of sub-queries pre-filled in the search box.
VITE_DEFAULT_SUBQUERIES=4
