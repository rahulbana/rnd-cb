# --- Web framework ---
fastapi>=0.111
uvicorn[standard]>=0.29
python-multipart>=0.0.9
pydantic>=2.6
pydantic-settings>=2.2
websockets>=12.0

# --- Document parsing ---
pymupdf>=1.24          # PDF text extraction (fitz)
pdfplumber>=0.11       # PDF tables
pytesseract>=0.3.10    # OCR (requires system 'tesseract' binary)
Pillow>=10.2           # image handling for OCR
python-docx>=1.1       # Word
python-pptx>=0.6.23    # PowerPoint
openpyxl>=3.1          # Excel
pandas>=2.2            # CSV / Excel dataframes

# Heavy, layout-aware / ML parsers (optional switchable backends).
# Uncomment to enable RAG_PDF_BACKEND=unstructured or =docling.
# unstructured[pdf]>=0.14
# docling>=1.0

# --- Chunking ---
tiktoken>=0.7          # accurate token counting

# --- ML backend (PyTorch) ---
# Declared explicitly so installs are reproducible. On Intel (x86) macOS pip
# resolves this to 2.2.2 automatically — the last PyTorch release with x86 mac
# wheels. Apple Silicon / Linux get a newer build (raise the cap there to use
# newer transformers below).
torch>=2.2

# --- Embeddings & reranking ---
# transformers/sentence-transformers are capped so they stay compatible with
# torch 2.2.2 — the last PyTorch release that supports Intel (x86) macOS.
# Newer transformers require torch>=2.5 and would disable PyTorch on those
# machines. On Apple Silicon / Linux you can raise these and use torch>=2.5.
sentence-transformers>=3.0,<3.3
transformers>=4.41,<4.46

# --- Vector store ---
chromadb>=0.5

# --- Retrieval ---
rank-bm25>=0.2.2       # sparse keyword retrieval for hybrid search

# --- LLM providers ---
openai>=1.30
httpx>=0.27            # Ollama streaming client
