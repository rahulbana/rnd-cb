# --- Core web framework ---
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2
email-validator>=2.1
python-multipart>=0.0.9

# --- Persistence ---
SQLAlchemy>=2.0
# PostgreSQL driver (used in production; SQLite works out of the box):
psycopg[binary]>=3.1 ; platform_system != "Windows"

# --- Security ---
python-jose[cryptography]>=3.3
passlib>=1.7

# --- LLM / HTTP ---
openai>=1.30
httpx>=0.27
tenacity>=8.2

# --- Testing ---
pytest>=8.0
pytest-asyncio>=0.23
