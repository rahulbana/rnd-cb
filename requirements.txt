fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
jinja2==3.1.4
pydantic==2.7.4
# Optional: only needed for the "Semantic (OpenAI embeddings)" strategy.
# Set OPENAI_API_KEY in the environment to enable it.
openai==1.35.7
