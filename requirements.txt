# Core runtime dependencies (offline pipeline, API, and CLI).
numpy>=1.24
pandas>=2.0
pydantic>=2.5
scikit-learn>=1.3
faker>=20.0
pyarrow>=14.0
openpyxl>=3.1
fastapi>=0.110
uvicorn[standard]>=0.27
typer>=0.9
pyyaml>=6.0
python-dotenv>=1.0
matplotlib>=3.7

# LLM-backed planner (OpenAI). Required only when PLANNER_BACKEND=llm.
openai>=1.30

# Testing.
pytest>=8.0
httpx>=0.27

# Optional extras (install with: pip install -e ".[llm]" or ".[synthetic]"):
#   anthropic, litellm     -> alternative LLM providers for the planner
#   sdv, imbalanced-learn  -> advanced synthetic data / class balancing
