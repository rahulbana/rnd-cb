# --- Core / LLM (the "thinking" agents) ---
openai>=1.40.0
pydantic>=2.7.0
python-dotenv>=1.0.0

# --- Article / blog extraction ---
requests>=2.31.0
trafilatura>=1.9.0
beautifulsoup4>=4.12.0

# --- Audio assembly ---
pydub>=0.25.1          # needs ffmpeg on the system
numpy>=1.24.0
soundfile>=0.12.1

# --- Open-source / free Text-to-Speech backends ---
# Default backend: Kokoro (Apache-2.0, open weights, multiple natural voices)
kokoro>=0.9.2
# Optional offline backend: Piper (MIT, fully local). Install if you want it:
#   pip install piper-tts
# piper-tts>=1.2.0
