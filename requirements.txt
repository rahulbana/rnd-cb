# --- Core orchestration ---
langgraph>=0.2.0
langchain-core>=0.3.0
langchain>=0.3.0                     # required by the Langfuse langchain integration
langchain-community>=0.3.0           # SQLite LLM response cache
langgraph-checkpoint-sqlite>=2.0.0   # optional SQLite checkpointer backend

# --- LLM providers (switchable) ---
langchain-openai>=0.2.0
langchain-anthropic>=0.2.0        # optional secondary provider

# --- Search providers (switchable) ---
tavily-python>=0.5.0
# serper uses plain HTTP, no dedicated SDK required

# --- Distributed task queue + resiliency ---
celery[redis]>=5.3.0
tenacity>=8.2.0

# --- Observability / tracing ---
langfuse>=2.53.0

# --- Scraping ---
httpx>=0.27.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# --- Models / config / CLI ---
pydantic>=2.6.0
pydantic-settings>=2.2.0
typer>=0.12.0
rich>=13.7.0
python-dotenv>=1.0.0
