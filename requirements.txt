# Runtime dependencies (mirror of pyproject.toml [project.dependencies])
# For editable install with dev extras use: pip install -e ".[dev]"
openai-agents>=0.2.0
openai>=1.55.0
mcp>=1.9.0
uvicorn>=0.30.0
tavily-python>=0.5.0
ddgs>=6.0.0
httpx>=0.27.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.0
rich>=13.7.0

# Optional: Langfuse tracing (or `pip install -e ".[observability]"`)
# langfuse>=3.0.0
# openinference-instrumentation-openai-agents>=0.1.0
