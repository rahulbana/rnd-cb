# Core deep-learning stack. MTCNN is a TensorFlow/Keras face detector that
# returns both bounding boxes and 5 facial landmarks, which we use to align masks.
tensorflow>=2.12,<2.17
mtcnn>=0.1.1

# Webcam capture, image processing and mask generation.
opencv-python>=4.7
numpy>=1.23
pillow>=9.4
