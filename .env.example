# Copy to .env (or export these in your shell) and fill in your key.
# The CLI reads OPENAI_API_KEY from the environment.

OPENAI_API_KEY=sk-your-key-here

# Optional: override the default model.
# OPENAI_MODEL=gpt-4o
