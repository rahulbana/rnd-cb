# Root .env consumed by docker-compose. Copy to .env and fill in.
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini

# TMDB — provide either the v4 read token (preferred) or the v3 API key.
MCP_TMDB_READ_ACCESS_TOKEN=
MCP_TMDB_API_KEY=
